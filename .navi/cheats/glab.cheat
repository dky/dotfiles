% glab

# glab - mr list
glab mr list

# glab mr approve
gh mr approve

# glab mr dev
glab mr create -b dev -a dky -t "Merge to Dev" -d "Merge branch changes to dev"

# glab mr d2m
glab mr create -s dev -b main -a dky -t "dev2main" -d "Merge changes from dev to main/master branch"
