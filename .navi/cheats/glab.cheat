% glab

# glab mr list
glab mr list

# glab mr approve
glab mr approve

# glab mr merge
glab mr merge

# glab mr dev b2d
glab mr create -b dev -a dky -t "Merge to Dev" -d "Merge branch changes to dev"

# glab mr main d2m
glab mr create -s dev -b main -a dky -t "Merge to Main" -d "Merge changes from dev to main branch"
