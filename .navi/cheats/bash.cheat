% misc

# bash aliases
$(__fzf_alias__)

# Create Empty files
echo '<content>' >> <file_name>

# heredoc, close with end
cat > <file> <<end

% sed

# find all files and replace string with sed
find . -type f -exec gsed -i 's/<search>/<replace>/g' {} +
